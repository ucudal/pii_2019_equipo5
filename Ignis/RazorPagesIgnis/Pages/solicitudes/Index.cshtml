@page
@model RazorPagesIgnis.Pages.solicitudes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Solicitud[0].RolRequerido)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Solicitud[0].NivelExperiencia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Solicitud[0].Observaciones)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Solicitud[0].HorasRealizadas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Solicitud) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RolRequerido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NivelExperiencia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Observaciones)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HorasRealizadas)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
