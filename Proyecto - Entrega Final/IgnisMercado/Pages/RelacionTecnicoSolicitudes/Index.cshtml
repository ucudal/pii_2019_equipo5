@page
@model IgnisMercado.Pages.RelacionTecnicoSolicitudes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RelacionTecnicoSolicitud[0].Tecnico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RelacionTecnicoSolicitud[0].Solicitud)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RelacionTecnicoSolicitud) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tecnico.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Solicitud.RolRequerido)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
