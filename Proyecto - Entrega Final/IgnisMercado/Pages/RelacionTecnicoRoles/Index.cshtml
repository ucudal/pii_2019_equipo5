@page
@model IgnisMercado.Pages.RelacionTecnicoRoles.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RelacionTecnicoRol[0].Tecnico)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RelacionTecnicoRol[0].Rol)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RelacionTecnicoRol) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tecnico.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rol.Nivel)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
